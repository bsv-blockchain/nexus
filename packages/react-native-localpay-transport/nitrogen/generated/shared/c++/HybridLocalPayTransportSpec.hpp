///
/// HybridLocalPayTransportSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif



#include <NitroModules/Promise.hpp>
#include <string>
#include <functional>

namespace margelo::nitro::localpaytransport {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `LocalPayTransport`
   * Inherit this class to create instances of `HybridLocalPayTransportSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridLocalPayTransport: public HybridLocalPayTransportSpec {
   * public:
   *   HybridLocalPayTransport(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridLocalPayTransportSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridLocalPayTransportSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridLocalPayTransportSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual bool isSupported() = 0;
      virtual std::shared_ptr<Promise<void>> startListening(const std::string& instanceName, const std::string& pskBase64, const std::function<void(const std::string& /* frameBase64 */)>& onFrame, const std::function<void(const std::string& /* message */)>& onError) = 0;
      virtual std::shared_ptr<Promise<void>> stopListening() = 0;
      virtual std::shared_ptr<Promise<void>> confirmFrame(bool accepted, const std::string& reason) = 0;
      virtual std::shared_ptr<Promise<std::string>> sendFrame(const std::string& instanceName, const std::string& pskBase64, const std::string& frameBase64, double timeoutMs, double connectTimeoutMs) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "LocalPayTransport";
  };

} // namespace margelo::nitro::localpaytransport
